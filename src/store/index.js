import { createStore } from 'vuex'
import side from './modules/side'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    side
  }
})
